// Example placeholder for data service.
// In a larger app, this would contain functions for fetching/posting readings, insights etc.

// const API_URL = process.env.REACT_APP_API_URL;

// export const getReadings = async (token) => {
//   const response = await axios.get(`${API_URL}/data/readings`, {
//     headers: { 'x-auth-token': token }
//   });
//   return response.data;
// };

// export const addReading = async (readingData, token) => {
//   const response = await axios.post(`${API_URL}/data/readings`, readingData, {
//     headers: { 'x-auth-token': token }
//   });
//   return response.data;
// };
